import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    display: "flex",
    gap: 10
  },
  book: {
    width: 100,
    height: 200,
    padding: 5,
    borderStyle: "solid",
    borderColor: "grey",
    borderWidth: 1
  }
});
