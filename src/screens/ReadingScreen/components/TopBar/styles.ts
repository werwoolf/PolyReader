import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    gap: 10
  },
  bookName: {
    width: "90%",
    fontSize: 20,
    fontWeight: "900",
    alignSelf: "center"
  }
});
