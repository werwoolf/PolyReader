import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  wrapper: {
    gap: 10,
    display: "flex"
  },
  header: {
    gap: 10,
    display: "flex",
    flexDirection: "row",
    alignItems: "center"
  },
  bookName: {
    fontSize: 18,
    fontWeight: "bold"
  },
  bookSize: {
    fontWeight: "bold"
  }
});
